# 재정비 1단계 후속 검증 — 5개 기능 실사용 가능 여부 / 탭 상세 / KEEP·IMPROVE·CONNECT·NEW

> 이 문서도 분석 전용입니다. DB 변경·코드 수정·마이그레이션·배포는 전혀 하지 않았습니다.

작성일: 2026-09-11

---

## 0. 먼저 바로잡을 부분

지난 `REORGANIZATION_ANALYSIS_PHASE1.md`에서 이 5개 기능(BQ2 검사, 알림/문자 자동발송, 결제/회계, 재등록 관리, 재검사 자동알림)은 **"이미 구현되어 있다"가 아니라 "코드 전체를 검색해도 전혀 존재하지 않는다(❌ 미구현)"**로 보고드렸습니다. 이번에 요청하신 4단계 분류 기준으로 다시 한 번 정확히 확인한 결과도 동일합니다 — 5개 전부 **④ 미구현**입니다. 아래는 각각 무엇을 확인했는지 근거까지 정리한 내용입니다.

| 기능 | 분류 | 확인한 근거 |
|---|---|---|
| BQ2 검사 | **④ 미구현** | 저장소 전체(`app/`, `lib/`, `components/`, `supabase/schema.sql`)에서 "BQ2" 문자열 검색 결과 **0건**. 관련 테이블·필드·화면·API 전혀 없음. 현재 검사 관련 테이블은 `reports`(서술형), `mtpris_reports`(다원재능), `brain_tests`(뇌기능검사) 3종뿐. |
| 알림/문자 자동발송 | **④ 미구현** | SMS/카카오알림톡 관련 외부 API(twilio·coolsms·aligo·NHN Cloud 등) 의존성이 `package.json`에 전혀 없음. 코드 내 발송 로직 검색 결과도 0건. 유일하게 존재하는 것은 `site_settings.kakao_url`인데, 이건 홈페이지의 "카카오톡으로 상담하기" 버튼(`components/home/ContactButton.tsx`)이 **관리자가 등록한 채널 상담 링크로 연결**해주는 정적 설정일 뿐, 시스템이 능동적으로 메시지를 보내는 기능이 아님. 학부모 결과지 링크 전달도 README에 "관리자가 직접 복사해서 문자/카카오톡으로 전달"이라고 명시되어 있어, **사람이 수동으로 하는 절차**임을 재확인. |
| 결제/회계 | **④ 미구현** | `bank_account`/`payment`/`invoice`/`ledger`/"회계"/"결제"/"장부"/"통장" 등 검색 결과 관련 코드 0건(지난 `ACCOUNTING_SYSTEM_ANALYSIS.md`에서 이미 확인, 이번에 재확인해도 동일). 금액(amount 등) 컬럼이 DB 30개 테이블 어디에도 없음. |
| 재등록 관리 | **④ 미구현** | "재등록"/enrollment/registration/잔여회차/총회차 검색 결과, 실제 코드에서는 매치가 전혀 없고 **이번에 제가 작성한 분석 문서 자기 자신**만 매치됨(즉 실제 구현 0건). `children`이나 다른 테이블에 시작일/종료예정일/회차/재등록여부 컬럼 없음. |
| 재검사 자동알림 | **④ 미구현** | `vercel.json` 자체가 저장소에 없어 Vercel Cron Job이 설정되어 있지 않음(자동 실행되는 배치/스케줄 작업이 시스템에 전혀 없음). "cron"/"scheduled" 검색에서 나온 매치는 전부 상담신청 상태값(`consult_scheduled`, "상담일정확정") 같은 무관한 문자열 우연 일치였음. 알림을 계산하거나 보낼 테이블·함수·API 전혀 없음. |

**결론**: 5개 기능 모두 코드/DB/화면 어느 층위에서도 발견되지 않았습니다 — "일부 구현" "코드만 있고 사용 어려움" 단계조차 아니고, 순수하게 **처음부터 새로 설계·개발해야 하는 신규 기능**입니다.

---

## 1. `service_type` / `class_day` — 절대 수정하지 않고, 실제 쓰이는 곳 전체 확인

말씀하신 대로 이번 검증에서도 **이 두 필드는 전혀 수정하지 않았습니다.** 저장소 전체에서 두 필드가 쓰이는 모든 위치를 확인했습니다.

**정확히 바로잡을 점 하나**: `service_type`/`class_day`는 로그인·세션·관리자 권한 판단 로직(`lib/auth.ts`의 `getCurrentActor`/`isFullAdmin`, `lib/data.ts`의 `actorCanAccessChild`)에는 **전혀 쓰이지 않습니다** — 그 로직들은 `staff.role`과 `child_staff_assignments`(담당 배정)만 봅니다. 다만 아래처럼 **"어떤 학부모에게 어떤 공지를 보여줄지"를 정하는 대상 필터링 로직**에는 실제로 쓰이고 있어서, 넓은 의미의 "누가 무엇을 볼 수 있는가"에 관여하는 건 맞습니다. 이 부분 때문에 "권한 체계"라고 느끼신 것 같고, 그 우려 자체는 타당합니다 — 그래서 절대 손대지 않는 게 맞습니다.

**실제 사용처 전체 목록**(코드 검색으로 확인):

| 파일 | 용도 |
|---|---|
| `lib/data.ts:1282-1299` (`matchesNoticeAudience`) | **학부모 공지 노출 대상 판정**. `audienceType==='program'`이면 `notice.audienceValue === child.serviceType` 문자열 완전일치로 그 공지를 보여줄지 결정. `audienceType==='weekday'`면 `classDay`에서 뽑은 요일 목록에 포함되는지로 결정. 이게 사실상 **가장 "권한 판단에 가까운" 실사용처**입니다. |
| `lib/data.ts:2291` (`getDashboardSummary`) | 관리자 홈 화면의 "오늘 수업 예정" 통계 숫자 계산(`parseClassDays(c.classDay)`로 오늘 요일과 매칭되는 아동 수 카운트) |
| `components/parent/ParentDashboard.tsx` | 학부모 마이페이지 홈에 "이용 프로그램"(serviceType 그대로 표시), "다음 수업일"(classDay로 계산) 표시 |
| `components/admin/ChildListSection.tsx` | 관리자 아동 목록 화면의 서비스종류 필터 드롭다운(입력된 값들에서 동적으로 목록 생성) |
| `components/admin/ChildInfoPanel.tsx` | 아동 기본정보 탭의 입력/표시 필드 자체(자유텍스트 입력창) |
| `components/admin/AttendanceCalendar.tsx` | 출결 달력에 요일별 예정 표시(색점) — `classDay` 기반 |
| `app/api/admin/children/route.ts`, `.../children/[id]/route.ts` | 아동 생성/수정 API의 입력 필드 통과 |
| `app/admin/parent-notices/page.tsx` | 공지 작성 화면에서 프로그램별/요일별 대상을 고를 때 쓰는 선택지 소스 |

**검토 결과 — 사업유형을 별도 필드/테이블로 분리하는 방향이 안전한가?**

**네, 안전하고 권장되는 방향입니다.** 이유:

1. `service_type`은 지금 **자유텍스트**라 고정된 코드가 아닙니다. "심리지원"과 "심리지원사업"처럼 사람이 다르게 입력하면 `matchesNoticeAudience`의 문자열 완전일치(`===`)가 실패해서, 공지가 의도한 아이에게 안 가는 사고가 이미 구조적으로 가능한 상태입니다. 회계·재등록까지 이 값에 의존하게 만들면 이 위험이 더 커집니다.
2. 그러니 **기존 `service_type` 컬럼은 절대 건드리지 않고 그대로 두되(공지 대상필터 등 기존 기능이 계속 이 값을 씀)**, 새로 "사업유형(정식 코드: 심리지원/비전형성/일반수업 등)"을 표현할 **별도 컬럼(예: `children.program_code`) 또는 별도 코드 테이블(`accounting_programs`류)**을 하나 더 추가하는 방식을 권장합니다. 이렇게 하면:
   - 기존 공지 대상 필터링(`service_type` 의존)은 한 글자도 안 바뀌어 그대로 동작
   - 새 필드는 고정 목록에서 선택하게 만들어 회계·재등록에 오탈자 없이 안전하게 연결 가능
   - 두 값이 당장은 사람이 보기에 "같은 걸 가리키는" 경우가 많더라도, 시스템적으로는 완전히 독립적이라 한쪽을 나중에 정리해도 다른 쪽이 깨지지 않음
3. `class_day`도 마찬가지로 그대로 두고, 지난 회차에 논의했던 "수업 시간표" 기능처럼 시간 정보가 필요해지면 **별도 컬럼(`class_time`)을 추가**하는 방향이 안전합니다(자유텍스트 파싱 방식을 바꾸지 않음).

---

## 2. 아동 상세페이지 10개 탭 — 정확한 이름과 실제 가능한 기능

(`app/admin/children/[id]/page.tsx` + `components/admin/ChildDetailTabs.tsx` 직접 확인)

| # | 탭 이름(코드상 key) | 실제 가능한 기능 |
|---|---|---|
| 1 | **기본정보** (`info`) | 아동 기본정보(이름/학년/생년월일/성별/보호자명/보호자연락처/서비스종류/수업요일/담당자/메모) 조회·수정. 같은 보호자 연락처를 쓰는 형제자매 그룹 표시(`FamilyGroupPanel`, guardian_phone 일치 기반). |
| 2 | **결과지** (`reports`) | 서술형 기질검사 + MT-PRIS 검사 이력을 검사일순으로 통합 나열. 각 건마다 수정/인쇄(PDF)/학부모 링크 발급·회수 가능. **단, 새 검사를 이 화면에서 등록하는 기능은 없음**(Supabase Table Editor에서 관리자가 직접 최초 행을 만들어야 함 — README에 명시된 기존 한계). |
| 3 | **출결·보강** (`attendance`) | 달력 UI로 날짜별 출석/결석 기록, 보강 여부·보강일 지정. 형제자매의 수업요일도 함께 표시(색점). |
| 4 | **뇌기능검사** (`brain`) | 검사 등록(파일 업로드 또는 수기 지표입력, 관리자만), 파일에서 텍스트/표/이미지 자동추출(사람 확인 후 확정), AI 해석 생성(키 설정 시), 학부모 공개용 요약 작성, 승인 처리. 선생님은 열람만 가능(`canEditBrainTests` 플래그로 등록/수정 버튼 숨김). |
| 5 | **검사변화비교** (`compare`) | 같은 검사종류의 뇌기능검사가 2건 이상일 때 지표별 시간에 따른 변화를 그래프+표로 비교. 지표 기준(정상범위/방향성)이 설정돼 있으면 개선/주의 판정, 없으면 숫자 증감만 표시. **뇌기능검사 전용**이며 MT-PRIS/서술형 검사는 이 비교 화면 대상이 아님. |
| 6 | **아동특성** (`traits`) | 기질/강점/보완점/주의사항/학습특성/정서행동특성/상담목표/선생님메모 입력(선생님도 담당아동이면 가능). AI에 실제로 전송할 항목을 고르는 화이트리스트 체크박스는 관리자만 조작 가능. |
| 7 | **뇌파훈련기록** (`eeg`) | 뇌파훈련 세션별 기록(날짜/소요시간/모드/단계/장비/측정지표/컨디션·참여도메모/관찰/특이사항/학부모코멘트) 등록·수정·삭제, 지표별 추이 그래프. |
| 8 | **활동사진** (`photos`) | 이 아동에게 사진 업로드(다른 아이와 함께 찍힌 사진은 태그로 공동 등록 가능), 사진 갤러리 조회. |
| 9 | **수업 코멘트** (`comments`) | 이 아동이 참여한 수업기록들의 코멘트 이력을 시간순으로 조회(읽기 전용 — 작성/수정은 "수업 간편입력" 화면에서 함, 이 탭 자체에 입력 기능 없음). |
| 10 | **월간리포트** (`monthly`) | 월별 성장리포트(참여도/강점/개선점/가정지도/다음달목표) 작성·수정, 학부모 공개 여부 설정. |

**참고**: "수업 코멘트" 탭은 이 아동 개인 화면에서 열람만 가능하고, 실제 작성은 `/admin/class-records/new`(수업 간편입력, 여러 아이를 한 번에 등록)에서 이루어집니다 — 요청하신 6번(수업기록 1분 이내 입력) 설계 시 이 진입점을 그대로 확장하는 것이 자연스럽습니다.

---

## 3. KEEP / IMPROVE / CONNECT / NEW 분류표

| 분류 | 기능 | 설명 |
|---|---|---|
| **KEEP** | 학부모 로그인(토큰/이름+비밀번호) | 그대로 유지 |
| **KEEP** | 마이페이지 8개 탭 전체 | 그대로 유지 |
| **KEEP** | 출결·보강 | 그대로 유지 |
| **KEEP** | 사진/앨범/센터소식 | 그대로 유지 |
| **KEEP** | 형제 연동(guardian_phone 기반) | 그대로 유지 — 정식 관계 테이블은 아니지만 지금 방식으로 계속 동작 |
| **KEEP** | RBAC(관리자/선생님), 감사로그 | 그대로 유지 |
| **KEEP** | MT-PRIS 검사 엔진 | 완성도 높음, 그대로 유지 |
| **KEEP** | 상담신청, 학부모 문의/건의 | 그대로 유지 |
| **IMPROVE** | 수업기록 입력값 | 참여도/집중도/이해도/정서상태/상호작용 등으로 세분화 (요청 6번) |
| **IMPROVE** | 수업기록 내부/공개 분리 | 지금은 comment 하나+공개스위치 → 내부기록/공개기록을 처음부터 별도 필드로 저장 (요청 7번) |
| **IMPROVE** | `children` 삭제 방식 | 하드삭제 → 소프트삭제 전환 검토(회계·재등록 연결 전 선행 권장) |
| **IMPROVE** | `staff.role` 권한 세분화 | admin/teacher 2단계 → 최고관리자/센터장·관리자/선생님/회계담당자 |
| **IMPROVE** | AI 기능 실가동 | 코드는 있으나 `ANTHROPIC_API_KEY` 미설정 — 키 설정 후 실제 품질 검증 |
| **CONNECT** | 뇌기능검사 ↔ 아동특성 ↔ 과거 수업기록 | 지금은 각 탭이 독립적 — AI 코멘트 초안(요청 6번)을 만들려면 이 세 데이터를 하나의 프롬프트로 연결해야 함 |
| **CONNECT** | 검사결과 ↔ 재등록 시점 | 재검사 예정일 계산 시 기존 `brain_tests`/`mtpris_reports`의 검사일 데이터를 참조하도록 연결 |
| **CONNECT** | `service_type`(기존) ↔ 사업유형(신규 필드) | 완전히 새로 만들지 않고, 신규 필드를 기존 값과 병행 운영하며 점진적으로 정리 |
| **CONNECT** | 아동 상세페이지 10개 탭 ↔ 재등록/프로그램 정보 | 새 "프로그램 등록" 탭을 추가해 기존 탭들과 나란히 배치 |
| **NEW** | BQ2 등 신규 검사종류 지원 | 완전 신규 |
| **NEW** | 초기/중간/최종 검사 비교 라벨링 | 완전 신규(현재 비교는 "종류가 같은 검사 여러 건"만 인식, 단계 개념 없음) |
| **NEW** | 재등록/프로그램 관리(`enrollments`류) | 완전 신규 |
| **NEW** | 재검사·변화보고서·재등록 자동 알림(스케줄 계산) | 완전 신규 |
| **NEW** | 알림/문자 자동발송(외부 채널 연동) | 완전 신규 — 외부 서비스(카카오 비즈메시지 등) 계약·연동 필요 |
| **NEW** | 회계 모듈 | 완전 신규(별도 문서) |
| **NEW** | 사업유형 코드 테이블 | 완전 신규(단, 기존 `service_type`은 유지) |

---

## 다음 지시를 기다립니다

이번에도 분석만 진행했고 코드/DB는 전혀 건드리지 않았습니다. 검토 후 다음 단계를 알려주세요.
