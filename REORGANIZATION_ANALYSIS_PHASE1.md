# 학습심리브레인센터 홈페이지 재정비 1단계 — 분석 보고서

> **이 문서는 분석·계획 전용입니다.** 이번 단계에서 코드 수정, 기능 구현, 마이그레이션 실행, 데이터/테이블 삭제, 배포는 전혀 하지 않았습니다. 아래는 전부 현재 저장소를 직접 읽어 확인한 사실이며, 실제 개인정보·계좌정보·비밀번호·API 키는 포함하지 않았습니다. **이 보고서를 먼저 검토해 주시고, 다음 지시를 주시면 그때 진행하겠습니다.**

작성일: 2026-09-11

---

## 0. 백업·버전관리 현황 확인 (요청하신 3번 항목)

| 확인 항목 | 결과 |
|---|---|
| Git 저장소 사용 여부 | 사용 중 (`origin`: `github.com/brain-jocheon/brain-center`) |
| 현재 브랜치 | `main`, 작업 트리 깨끗함(이번에 만든 분석 문서 2개만 미추적 상태) |
| origin과의 동기화 상태 | `main`이 `origin/main`과 완전히 동기화됨(밀리거나 앞서 있지 않음) |
| 최근 커밋 | 가장 최근 커밋은 "선생님 계정 비밀번호 재설정 기능 추가" — 그 앞으로 보안 취약점 패치, 감사로그 도입 등 최근 세션에서 이어온 작업들이 순서대로 남아있음(커밋 메시지 자체에 민감정보 없음) |
| 환경변수 파일 | `.env.local`(실제 값 보유, `.gitignore`에 의해 저장소에는 절대 커밋되지 않음 — 확인됨) / `.env.example`(값 없는 템플릿, 저장소에 포함되어도 안전) 둘 다 존재 |
| 데이터베이스 백업 방법 | **애플리케이션 코드 안에는 별도 백업 스크립트가 없음.** Supabase(관리형 Postgres)의 자동 백업/PITR 기능에 전적으로 의존하는 구조 — 백업 보관기간은 Supabase 요금제에 따라 다르므로, **대시보드에서 현재 플랜의 백업 정책을 직접 확인**해야 정확히 알 수 있음(코드로는 확인 불가) |
| 업로드 파일 백업 방법 | Supabase Storage(사진/검사파일) 역시 Storage 자체의 백업 정책에 의존 — 별도의 애플리케이션 차원 백업(예: 정기적으로 다른 곳에 복사)은 코드에 없음 |
| 백업 관련 스크립트 | `scripts/` 폴더에는 `build-content.js`(콘텐츠 빌드), `extract-mtpris.js`(데이터 추출)만 있고 백업 전용 스크립트는 없음 |

**제안**: 실제 수정 작업을 시작하기 전에, (1) Supabase 대시보드에서 현재 백업 보관 기간을 확인하시고, (2) 큰 변경(마이그레이션 등)을 하는 날에는 수동으로 한 번 더 스냅샷/백업을 받아두시는 것을 권장합니다. 이번 세션에서는 마이그레이션이나 데이터 변경을 전혀 하지 않았으므로 백업이 당장 필요한 상황은 아닙니다.

---

## A. 현재 시스템 요약

**기술 구조**: Next.js 14.2.35(App Router) + TypeScript + Tailwind CSS, Supabase(Postgres + Storage)를 REST API로만 사용(직접 DB 접속 없음), Vercel + GitHub 자동배포. 자체 구현한 인증(bcrypt 해시 + HMAC 서명 쿠키), Anthropic(Claude) API를 SDK 없이 직접 `fetch`로 호출하는 AI 어댑터 계층. 자동화 테스트는 없고, 지금까지 모든 기능을 "빌드 타입체크 + 실제 운영 DB에 임시 데이터로 수동 검증" 방식으로 개발해왔습니다.

**프런트엔드 구조**: `app/`(라우트) + `components/`(화면조각, admin/home/mtpris/parent로 폴더 분리) 구조. 별도 상태관리 라이브러리 없이 React 기본 state + 서버 컴포넌트에서 데이터를 미리 가져와 props로 내려주는 방식.

**백엔드 구조**: Next.js API 라우트(`app/api/**`)가 전부이며, 별도 백엔드 서버는 없음. 데이터 접근은 `lib/data.ts`(2,294줄) 한 파일에 전부 모여 있고, 화면·API는 이 파일의 함수만 호출합니다("화면 — 데이터접근 — 타입" 3단 구조가 프로젝트 전체에서 일관되게 지켜짐).

**핵심 이용자 4주체**: ① 홈페이지 방문자(로그인 불필요), ② 학부모(토큰+비밀번호, 완전 무상태 로그인), ③ 관리자/선생님(RBAC, 세션 쿠키), ④ (현재는 없음) 회계담당자.

---

## B. 현재 데이터베이스 구조

Supabase(Postgres), **테이블 30개**, 전부 RLS 켜짐 + 정책 없음(서비스 키로만 접근, 실제 접근 제어는 애플리케이션 코드 담당). 아동(`children`)을 중심으로 대부분의 테이블이 `child_id`로 연결되는 구조입니다.

```
children (허브 테이블)
 ├─ reports, mtpris_reports ─ access_tokens ─ access_logs   (검사결과 + 학부모 링크)
 ├─ brain_tests                                              (뇌기능검사, AI해석 필드 포함)
 ├─ eeg_training_sessions, eeg_test_templates                (뇌파훈련 + 검사기준)
 ├─ attendance_records, makeup_requests                      (출결 + 보강신청)
 ├─ activity_photos ↔ photo_students                         (사진, 다대다)
 ├─ class_records ↔ class_record_children → child_comments   (수업기록 + 아이별 코멘트)
 ├─ monthly_reports                                          (월간 성장리포트)
 ├─ parent_notices ↔ parent_notice_reads                     (학부모 대상별 공지)
 ├─ parent_feedback                                          (학부모 문의/건의)
 ├─ child_traits                                             (아동 특성, AI 참고용)
 └─ child_staff_assignments → staff                          (담당 선생님 배정, RBAC)

부가:
 - consultations            (공개 홈페이지 상담신청, children과 무관 — 아직 등록 안 된 잠재고객)
 - site_settings, notices    (로그인 불필요 공개 홈페이지 콘텐츠)
 - page_views                (익명 방문 통계)
 - ai_generation_logs        (AI 호출 이력, 개인정보는 해시만)
 - audit_logs                (관리자 작업 감사로그 — 범용, 어떤 테이블 작업이든 기록 가능)
 - comment_templates         (자주 쓰는 코멘트 문구)
```

**아동(`children`) 핵심 컬럼**: id, name, grade, birth_year/date, status(active/waiting/ended), gender, guardian_name, **guardian_phone**(관리자 전용, 학부모 화면·URL에 절대 미노출), service_type(자유텍스트), class_day(자유텍스트, 요일만), counselor, memo.

**중요한 구조적 특징**: `service_type`과 `class_day`는 둘 다 **고정 목록이 아닌 자유텍스트**입니다. 화면의 필터 목록도 "지금까지 입력된 값들의 집합"을 그때그때 계산하는 방식이라, 지금 "심리지원사업"/"비전형성사업" 같은 사업 구분은 시스템적으로 강제되는 개념이 아니라 사람이 텍스트로 적어놓은 것에 불과합니다. 4번 항목에서 요청하신 통합 구조를 만들려면 이 부분을 구조화(고정 코드 테이블화)하는 작업이 필요합니다.

**형제자매 연결**: 정식 FK 관계가 아니라 `guardian_phone` 문자열이 같은 아이들을 그때그때 찾아 묶는 방식(`getSiblingChildIds()`, `lib/data.ts`). 즉 "형제 관계"라는 데이터가 별도로 저장되지 않고, 매번 계산합니다.

---

## C. 현재 기능 목록 (정상작동 / 미완성 / 미사용 구분)

| 기능 | 상태 | 비고 |
|---|---|---|
| 학부모 로그인(토큰+비밀번호) | ✅ 정상 | `app/report/[token]`, 완전 무상태, bcrypt 검증 |
| 학부모 이름+비밀번호로 바로 찾기 | ✅ 정상 | 형제자매까지 한번에 인식(`/api/report/find-family`), 레이트리밋+일반화된 오류메시지 적용됨(보안 점검 완료) |
| 마이페이지(학부모 대시보드) | ✅ 정상 | `ParentDashboard.tsx` — 홈/출결/사진/검사자료/성장기록/공지/결과지/문의 8개 탭, 형제전환 지원 |
| 아동 정보 관리(관리자) | ✅ 정상 | 등록/수정/삭제(단, 삭제는 하드삭제 — D번에서 다룸) |
| 선생님/관리자 계정(RBAC) | ✅ 정상 | 최근 세션에서 비밀번호 재설정 기능까지 추가 완료 |
| 출결 관리 | ✅ 정상 | 날짜별 출석/결석 + 보강일 지정, 달력 UI(라이브러리 없이 직접 구현) |
| 보강 희망일 신청 | ✅ 정상 | 학부모 제안 → 관리자 승인/거절(`makeup_requests`) |
| 수업기록 + 아이별 코멘트 | ✅ 정상 | 날짜+활동명+참여아동+공용코멘트+아이별 오버라이드, AI 초안 기능 있음(6번 참고) |
| 사진 업로드/앨범 | ✅ 정상 | 비공개 Storage + 서명URL, 아이 다대다 태그, "센터소식" 전체공개 옵션 별도 |
| 검사자료 업로드/조회 — 서술형 기질검사 | ✅ 정상(등록은 관리자 수기) | 관리자가 Supabase Table Editor로 최초 행을 만들어야 하는 한계가 README에 남아있음 |
| 검사자료 — 다원재능(MT-PRIS) | ✅ 정상, 완성도 높음 | 자동 생성 엔진, 학부모 마스킹 별도 처리, 인쇄용 A4 시트 |
| 검사자료 — 뇌기능검사(brain_tests) | ✅ 정상 | 파일 업로드(PDF/엑셀/이미지) → 텍스트/표 추출 → **사람이 확인해야만** 확정 지표 반영 → AI 해석(선택) → 검토 → 학부모 공개. 검사 전후 비교 화면도 있음(같은 검사종류 내에서만) |
| **BQ2 검사** | ❌ 없음 | 코드 전체에서 "BQ2" 매칭 없음 — 완전히 새로 만들어야 함 |
| PDF 저장/다운로드 | ✅ 정상(브라우저 인쇄 방식) | 서버가 PDF를 생성하는 라이브러리(jsPDF 등)는 없고, A4 스타일 인쇄 전용 화면 → 브라우저 인쇄창 → "PDF로 저장" 방식. 결과지(서술형/MT-PRIS) 인쇄 화면 존재 |
| 형제 계정 연결 | ✅ 정상(단, 정식 관계 테이블 아님) | `guardian_phone` 일치로 매번 계산(위 B번 참고) |
| 학부모 문의/건의 | ✅ 정상 | `parent_feedback`, 관리자 답변 가능 |
| 학부모 대상별 공지 | ✅ 정상 | 전체/상태별/프로그램별/요일별/개별아동 타겟팅 |
| 상담 신청(신규 방문자) | ✅ 정상 | 공개 홈페이지 폼 → 관리자 파이프라인(7단계 상태) |
| 감사로그 | ✅ 정상(최근 추가) | 로그인 성공/실패, 계정 생성/활성화, 삭제 등 보안 관련 이벤트 |
| **알림/문자 자동발송** | ❌ 없음(수동) | 학부모 링크는 관리자가 문자/카카오톡으로 **직접 복사해서 전달**하는 방식. 자동 발송(카카오 알림톡 API 등)은 README의 "향후 확장 아이디어"에만 적혀있고 구현된 적 없음 |
| **결제/회계 기능** | ❌ 전혀 없음 | 지난 세션에서 별도로 분석 완료(`ACCOUNTING_SYSTEM_ANALYSIS.md`) — 금액 관련 컬럼이 DB 어디에도 없음 |
| **재등록/프로그램 등록내역 관리** | ❌ 없음 | 시작일/종료예정일/회차/재등록 여부를 저장하는 테이블이 전혀 없음 |
| **재검사/변화보고서 자동 흐름** | ❌ 없음 | 뇌기능검사의 "검사변화비교" 화면은 있지만, 이는 이미 입력된 여러 검사를 비교해서 "보여주는" 기능일 뿐, 재검사 시점을 알려주거나 보고서를 자동 생성하는 기능은 아님 |
| AI 연동 | ✅ 정상(부분적) | 수업기록 AI 초안, 뇌기능검사 AI 해석, 이미지/스캔PDF 인식 3가지 기능이 Claude API로 구현되어 있음. **다만 운영 환경에 `ANTHROPIC_API_KEY`가 아직 설정되지 않아, 실제로는 전부 "준비중" 상태로 동작 중**(코드는 완성, 키만 없음) |

---

## D. 문제점

1. **`service_type`/`class_day`가 자유텍스트라 사업·시간 구조화가 안 되어 있음** — 4번 항목이 요청한 "사업별 관리"의 전제조건이 지금 없음(B번 참고).
2. **아동 삭제가 하드삭제(완전삭제)** — 지난 보안점검에서 이미 발견된 기존 gap. 아동을 지우면 관련 기록(검사·출결·사진 등)이 FK cascade로 함께 사라지거나(관계에 따라 다름) 고아 데이터가 될 수 있고, 원클릭 복구가 불가능함(삭제 직전 스냅샷만 감사로그에 남음). 향후 회계·재등록 데이터까지 연결되면 이 문제의 위험도가 더 커짐.
3. **수업기록의 "내부/공개" 분리가 완전하지 않음** — `child_comments`에 `is_public_to_parent`(공개여부 플래그)와 `strengths_note`/`difficulties_note`/`teacher_memo`(관찰용 필드), AI 초안 3분할(`ai_draft_detail`/`ai_draft_parent`/`ai_draft_guidance`)까지는 있지만, **최종 저장되는 필드는 사실상 `comment` 하나이고 이걸 공개할지 말지만 스위치로 정하는 구조**입니다. 7번 항목이 요청한 "내부기록 전체와 학부모 공개기록을 처음부터 분리 저장"하는 형태는 아직 아님(자세한 내용은 F번 참고).
4. **`lib/data.ts`가 2,294줄로 계속 커지고 있음** — 지금까지는 기능별 섹션으로 잘 정리되어 있지만, 회계·재등록·검사확장까지 여기에 다 더하면 유지보수가 어려워질 수 있음(F번에서 분리 제안).
5. **자동화 테스트가 전혀 없음** — 모든 검증이 수동(빌드 타입체크 + 운영 DB 수동 테스트)이라, 기능이 많아질수록 회귀 위험이 커짐.
6. **최근 세션에서 발견·수정한 보안 문제 중 아직 남은 것들**(이미 사용자께 별도 보고드린 목록, 여기선 요약만): 사진 수정 API의 권한 재검증 일부 누락, 공용 관리자 계정으로 로그인 시 AI 사용량 제한이 적용 안 되는 점, IP 기반 요청제한이 프록시 헤더에 의존하는 점, 학부모 형제확인 API 일부에 요청제한 누락, 업로드 파일의 실제 내용(매직바이트) 검증 부재, 코멘트 템플릿 소유권 검사 없음, 감사로그의 개인정보 보관기간 정책 부재. **Critical/High 항목(Next.js 취약점, 관리자 화면 이중인증, IDOR, SESSION_SECRET 하드코딩)은 이미 조치 완료했습니다.**
7. **`class_day` 자유텍스트 파싱의 한계** — "월,수,금" 같은 텍스트에서 요일 글자만 스캔하는 방식이라, 오탈자나 다른 표현("월요일마다")은 파싱이 안 될 수 있음. 시간 정보는 아예 없음.
8. **권한 구조가 요청하신 4단계(최고관리자/센터장·관리자/선생님/학부모)보다 단순함** — 지금은 사실상 "전체관리자(admin)"와 "선생님(teacher)" 2단계뿐이고, "최고관리자"와 "센터장/관리자"를 구분하는 개념이 없음(I번에서 자세히 다룸).

---

## E. 유지해야 할 기능 (이번 재정비에서도 반드시 보존)

요청하신 목록을 전부 확인했고, **모두 현재 정상 작동 중**이며 이번 재정비에서 구조나 데이터를 깨지 않고 보존해야 합니다.

- 학부모 로그인(토큰+비밀번호, 이름+비밀번호 찾기 포함)
- 마이페이지(8개 탭 전체, 형제전환 포함)
- 아동별 출결(달력, 보강일 표시)
- 사진(앨범, 센터소식, 아이 태그)
- 코멘트(수업기록 아이별 코멘트, 공개여부 스위치)
- 보강 변경(학부모 신청 → 관리자 승인)
- 건의 기능(`parent_feedback`, 답변 포함)
- 검사 PDF(서술형/MT-PRIS 인쇄 화면, 브라우저 인쇄 방식)
- 형제 연동(guardian_phone 기반 자동 인식)
- 관리자 기능(아동관리, 검사관리, 계정관리, 감사로그, 홈페이지관리 등 전체)
- 선생님용 기능(담당아동 조회, 출결·수업기록 입력, 사진 등록, AI 초안 확인 — 단 담당 아동 범위 안에서)

이 목록에 있는 화면·API·테이블은 이번 재정비 과정에서 **구조를 바꾸더라도 반드시 마이그레이션 경로(기존 데이터 호환)를 먼저 설계한 뒤에만** 손대야 합니다.

---

## F. 변경이 필요한 기능 (이유와 함께)

| 기능 | 왜 바꿔야 하는가 |
|---|---|
| `service_type`(자유텍스트) → 사업 코드 테이블 | 4번(아동 상세 통합), 10번(회계, 프로그램별 손익) 요건 모두 "정확한 사업 구분"을 전제로 함. 자유텍스트는 오탈자·중복 표기 위험이 있어 정부 보고·회계 정산과 직결되는 지금 단계에서는 구조화가 필요 |
| `class_day` → 요일+시간 구조화 | "수업 스케줄"(4번)을 화면에 제대로 보여주려면 최소한 시간 정보가 있어야 함(현재는 요일만) |
| `child_comments`의 내부/공개 필드 재설계 | 7번 요건대로 "내부기록"과 "학부모 공개기록"을 애초에 별도 필드로 나눠 저장해야, 실수로 내부 관찰내용이 공개되는 사고를 원천 차단할 수 있음(지금은 하나의 comment를 스위치로 가리는 구조라 완전히 안전하다고 보기 어려움) |
| `children` 삭제를 소프트삭제로 전환 | 회계·재등록 데이터까지 연결되면 하드삭제의 위험이 커짐(D-2) — 다만 이건 전체 목록 쿼리 수정이 필요한 큰 변경이라 신중한 단계별 접근 필요 |
| `lib/data.ts` 파일 분리 | 지금처럼 한 파일에 계속 추가하면 유지보수가 어려워짐 — 이미 `lib/mtpris/`, `lib/extraction/`처럼 기능별 하위 폴더로 분리한 전례가 있으므로, 새 기능(수업기록 확장, 검사확장, 재등록, 회계)은 처음부터 `lib/accounting.ts`, `lib/enrollment.ts` 등으로 분리 권장 |
| `staff.role` 체크 제약 확장 | 지금은 `'admin' | 'teacher'` 둘뿐이라 5번이 요청한 "최고관리자/센터장·관리자/회계담당자" 세분화를 담을 수 없음 |

---

## G. 신규 개발이 필요한 기능 (우선순위와 함께)

**P1(우선)**
- 수업기록 입력값 확장(참여도/집중도/이해도/정서상태/상호작용/어려운점/잘한점/특이사항/다음목표/자유메모) — H번에서 구체적 필드안 제시
- 내부기록/학부모공개기록 명확한 2계층 저장 구조

**P2**
- 검사시스템 확장(BQ2 등 다검사 지원, 검사종류 자동판별, 초기/중간/최종 비교 라벨링)
- 재등록/프로그램 관리(`enrollments` 테이블 신설, 종료예정일 계산, 잔여회차)
- 재검사·변화보고서·재등록 자동 알림(스케줄 기반, 실제 발송은 별도 채널 연동 필요)
- AI 코멘트 초안(이미 어댑터는 있음 — `ANTHROPIC_API_KEY` 설정 후 아동특성+검사결과+과거기록을 프롬프트에 포함하도록 확장)

**P3**
- 회계 모듈(별도 분석 문서 존재)
- 통계/대시보드 고도화

---

## H. 추천 데이터 구조 — 아동 중심 통합 구조

4번 항목이 요청한 "아동 1명 상세화면에서 모든 정보를 연결해서 보기"는 **이미 상당 부분 가능**합니다(현재 아동 상세 화면에 탭 10개: 기본정보/결과지/출결/뇌기능검사/검사비교/아동특성/뇌파훈련/사진/코멘트/월간리포트). 아래는 **부족한 부분만** 채우는 확장안입니다(신규 테이블 이름은 가안).

```
children (기존, 확장: service_program_id로 accounting_programs 참조 — 회계분석 문서와 동일한 사업코드 테이블 재사용)
 │
 ├─ enrollments (신규) ─ 프로그램 등록내역
 │    child_id, program_id, start_date, end_date_expected,
 │    total_sessions, used_sessions, remaining_sessions,
 │    re_test_due_date, parent_consult_due_date, re_enrolled(boolean), status
 │
 ├─ class_records/child_comments (기존 확장)
 │    + 내부전용 필드(관찰/특이사항/상담참고) vs 공개전용 필드(오늘활동/긍정반응/변화/다음목표/가정참고) 명확히 분리
 │    + 참여도/집중도/이해도/정서상태/상호작용을 각각 별도 컬럼 또는 소분류 jsonb로
 │
 ├─ test_sessions (신규, 검사 공통 상위 개념 — 선택) 또는 기존 brain_tests/mtpris_reports 각각에
 │    test_stage 컬럼 추가('initial'|'mid'|'final')로 "초기/중간/최종" 비교를 명시적으로 지원
 │
 ├─ notification_schedules (신규, 9번 요건)
 │    child_id, enrollment_id, trigger_type('retest_reminder'|'change_report_prep'|'reenrollment_notice'),
 │    trigger_date(계산값: end_date_expected - 30/14/7일), sent_at, status
 │
 └─ (회계는 별도 모듈 — ACCOUNTING_SYSTEM_ANALYSIS.md 참고, children에는 FK만 연결)
```

**설계 원칙**: 기존 `brain_tests`/`mtpris_reports`/`class_records` 등은 그대로 두고 **컬럼 추가(add column if not exists) 또는 신규 테이블 추가**로만 확장 — 지금까지 6단계, 14단계 등에서 실제로 써온 방식과 동일합니다.

---

## I. 권한 설계

**현재 구조**: `CurrentActor`가 `legacy_admin`(공용 관리자 비밀번호) 또는 `staff`(role: `'admin' | 'teacher'`) 두 갈래뿐입니다. `isFullAdmin()`은 `legacy_admin`이거나 `staff.role==='admin'`이면 true — 즉 **"최고관리자"와 "센터장/관리자"가 지금은 구분되지 않고 똑같이 취급**됩니다. 선생님(`teacher`)은 `actorCanAccessChild()`로 담당 배정된 아동만 접근 가능하도록 이미 잘 구현되어 있습니다. 회계담당자 개념은 전혀 없습니다.

**요청하신 4단계와의 격차**:

| 요청 권한 | 현재 상태 |
|---|---|
| 최고관리자(전체+회계+사용자관리+AI승인) | `legacy_admin` 또는 `staff.role='admin'`이 사실상 이 역할을 겸함 |
| 센터장/관리자(아동/수업/상담/검사/재등록/일부회계) | **별도 역할 없음** — 지금은 admin이면 전체 다 됨, "일부 회계만" 같은 세분화 불가 |
| 선생님(담당아동만) | ✅ 이미 구현됨(`teacher` role) |
| 학부모(본인자녀만) | ✅ 이미 구현됨(토큰 기반 무상태 접근) |

**개선 제안**: `staff.role`을 `'admin' | 'manager' | 'teacher' | 'accountant'` 4단계로 확장(체크 제약 재정의)하고, `isFullAdmin()` 옆에 `isManager()`, `canAccessAccounting()` 같은 세분화된 권한 함수를 추가하는 방식을 권장합니다. `legacy_admin`(공용 비밀번호)은 항상 "최고관리자"와 동급으로 유지하는 것이 기존 로그인 방식을 깨지 않는 안전한 접근입니다.

---

## J. 개발 우선순위

- **P0 (기존 서비스 안정성과 보안)**: D번에서 정리한 남은 보안 항목 마무리, `children` 소프트삭제 전환 검토, 자동화 테스트 최소 도입 검토
- **P1 (수업기록 및 아동 데이터 구조)**: 수업기록 입력값 확장 + 내부/공개 2계층 분리(F, G, H번), `service_type`/`class_day` 구조화
- **P2 (AI / 검사 / 재등록)**: 검사시스템 확장(BQ2 등), 초기/중간/최종 비교, `enrollments` 테이블 + 재등록 흐름, AI 코멘트 초안 실제 가동(API 키 설정 포함), 알림 스케줄(발송은 채널 연동 별도 결정 필요)
- **P3 (회계 / 고급 통계 / 추가 자동화)**: 별도 분석 문서(`ACCOUNTING_SYSTEM_ANALYSIS.md`) 기준으로 진행, 통계 대시보드 고도화

---

## K. 예상 위험요소

- **기존 기능 파손**: `service_type`/`class_day`를 자유텍스트에서 구조화된 값으로 바꿀 때, 기존에 이미 입력된 자유텍스트 데이터와의 매핑을 잘못하면 기존 아동의 프로그램 정보가 깨질 수 있음 — 반드시 기존 값 전수조사 후 매핑표를 먼저 만들어야 함.
- **데이터 유실**: `children` 삭제 방식을 바꾸는 작업, 또는 `class_day` 컬럼 형식을 바꾸는 작업 모두 되돌리기 어려운 변경 — 실행 전 스냅샷/백업 필수(0번 항목 참고).
- **개인정보 문제**: 재등록·회계 데이터가 아동에 연결되면서, 권한 설계(I번)를 세분화하지 않으면 회계담당자나 특정 관리자가 원래 볼 필요 없는 심리검사 자료까지 함께 노출될 위험.
- **마이그레이션 위험**: Supabase는 직접 Postgres 연결이 없어 스키마 변경을 SQL Editor에서 **사람이 수동으로 실행**해야 함 — 애플리케이션 코드는 새 컬럼이 이미 있다고 가정하고 배포되는데 마이그레이션 실행이 늦어지면 그 사이 에러가 날 수 있음(지금까지도 이 방식으로 진행해왔고, `try/catch`로 마이그레이션 전에도 화면이 깨지지 않게 방어하는 패턴을 계속 써왔음 — 앞으로도 동일하게 적용 필요).
- **AI 기능 확장 시 개인정보 전송 범위 확대**: 수업기록 AI 초안에 아동특성+과거기록까지 포함시키면, 지금보다 더 많은 개인정보가 Claude API로 전송됨 — 화이트리스트 방식(`child_traits.ai_include_fields`)을 계속 유지·확장해야 함.

---

## L. 실제 개발 순서 (작은 단위로 제안)

큰 재작성 없이, 지금까지 이 프로젝트가 해온 것과 동일한 "작은 단위 + 매 단계 검증" 방식을 제안합니다.

1. **결정 사항 확정**: 사업 코드 목록 확정, 권한 4단계 세부 정의, `enrollments` 필드 확정(회계 분석 문서의 12번 결정사항과 함께 검토)
2. **스키마 확장(1)**: 사업 코드 테이블 + `children.service_program_id` 추가(기존 `service_type`은 유지, 병행)
3. **권한 확장**: `staff.role` 4단계로 확장 + 세분화된 권한 함수(화면 변경 없이 구조만)
4. **수업기록 확장**: 입력값 세분화 + 내부/공개 필드 분리(기존 데이터 호환 유지)
5. **검사시스템 확장 준비**: BQ2 등 새 검사종류를 위한 스키마(화면 없이 구조만 먼저, 6단계 때와 동일한 방식)
6. **재등록 관리**: `enrollments` 테이블 + 기본 CRUD 화면(알림 자동화는 이후 단계)
7. **알림 스케줄 계산 로직**: 발송 없이 "언제 알려야 하는지" 계산만 먼저(발송 채널은 별도 결정 후)
8. **AI 코멘트 초안 실가동**: `ANTHROPIC_API_KEY` 설정 후 기존 어댑터로 실제 생성 품질 확인
9. **회계 모듈**: 별도 문서 기준으로 독립 진행

각 단계마다 지금까지처럼 `npm run build` 타입체크 → 로컬 실제 프로덕션 빌드 검증 → 운영 DB에 임시 데이터로 수동 확인 → 배포 → 프로덕션 재확인 순서를 유지할 것을 권장합니다.

---

## 다음 지시를 기다립니다

이번 단계에서는 위 내용을 정리하는 것 외에 어떤 코드도 수정하지 않았고, 마이그레이션·삭제·배포도 하지 않았습니다. 위 보고서를 검토해 주시고, 어느 항목부터(예: 결정 사항 확정, 특정 단계 착수 등) 진행할지 알려주시면 그에 맞춰 다음 계획을 세우겠습니다.
